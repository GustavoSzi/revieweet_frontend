export type ContainerTypes = {
    children?: React.ReactNode,
    customStyles?: string
}

export type RowTypes = {
    children?: React.ReactNode,
    customStyles?: string
}

export type ColumnTypes = {
    desktop?: number,
    medium?: number,
    tablet?: number,
    phone?: number,
    children?: React.ReactNode,
    customStyles?: string
}